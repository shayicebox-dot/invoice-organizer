# `src/types` — shared types

Cross-layer TypeScript types that do not belong to a single module: domain
primitives, generated Supabase types (`database.ts`, once the schema exists),
and shared enums.

Types that are specific to one module live with that module.
